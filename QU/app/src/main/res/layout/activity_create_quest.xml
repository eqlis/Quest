<?xml version="1.0" encoding="utf-8"?>
<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:columnCount="2"
    android:orientation="horizontal"
    tools:context=".CreateQuestActivity">

    <TextView  android:text="@string/title_text_view" />
    <EditText
        android:id="@+id/titleEditText"
        android:hint="@string/enter_quest_title"
        android:inputType="text"
        android:autofillHints="" />

    <TextView android:text="@string/location_text_view" />
    <EditText
        android:id="@+id/locationEditText"
        android:hint="@string/enter_location"
        android:inputType="text"
        android:importantForAutofill="no" />

    <Button android:text="@string/save_button" />
    <Button android:text="@string/cancel_button" />

</GridLayout>